<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode
  setting is off -->
  <style name="LaunchTheme" parent="Theme.AppCompat.DayNight">
    <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
    <item name="android:windowBackground">@drawable/launch_background</item>
    <item name="android:forceDarkAllowed">false</item>
    <item name="android:windowFullscreen">false</item>
    <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
  </style>
  <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
  <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
    <item name="android:windowBackground">?android:colorBackground</item>
  </style>

  <style name="Theme.Immich.UltraHdrViewer" parent="Theme.Material3.DayNight.NoActionBar">
    <item name="android:windowAnimationStyle">@style/AnimationImmichNoAnimation</item>
  </style>

  <style name="AnimationImmichNoAnimation" parent="@android:style/Animation">
    <item name="android:activityOpenEnterAnimation">@anim/immich_no_animation</item>
    <item name="android:activityOpenExitAnimation">@anim/immich_no_animation</item>
    <item name="android:activityCloseEnterAnimation">@anim/immich_no_animation</item>
    <item name="android:activityCloseExitAnimation">@anim/immich_no_animation</item>
    <item name="android:taskOpenEnterAnimation">@anim/immich_no_animation</item>
    <item name="android:taskOpenExitAnimation">@anim/immich_no_animation</item>
    <item name="android:taskCloseEnterAnimation">@anim/immich_no_animation</item>
    <item name="android:taskCloseExitAnimation">@anim/immich_no_animation</item>
  </style>
</resources>
